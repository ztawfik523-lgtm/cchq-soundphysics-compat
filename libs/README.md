# Local reconstruction dependency

The tested CC:HQ Speakers artifact is not currently resolved from a public Maven repository by this reconstruction.

Place the exact tested jar here as:

`hqspeaker-1.0.1-Neoforge1.21.1.jar`

Runtime metadata inside that jar reports CC:HQ Speakers `1.1.4-neoforge-1.21.1`.

Do not commit the jar to this repository unless we deliberately decide to vendor it later.
