org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# prepareSprCompileJar intentionally resolves the pinned third-party SPR/AT inputs
# at task execution time. Release validation builds use the configuration cache
# disabled so the isolated compile-only SPR transform stays reproducible.
org.gradle.configuration-cache=false

# Build baseline
gradle_version=9.2.1
minecraft_version=1.21.1
neo_version=21.1.248
cc_tweaked_version=1.120.2
spr_modrinth_version_id=Dd2tmpsk
cloth_config_curse_file=5729127

# HQ's runtime metadata reports 1.1.4-neoforge-1.21.1, while the tested file is
# the Modrinth NeoForge 1.21.1 artifact pinned below. Immutable project/version
# IDs keep builds reproducible regardless of the embedded display version.
hqspeaker_modrinth_project_id=ygA78R8l
hqspeaker_modrinth_version_id=u5PEI5Ax

# Mod identity
mod_id=cchq_soundphysics_compat
mod_name=CC:HQ Sound Physics Compat
mod_license=MIT
mod_version=0.1.0-beta11-rc1
mod_group_id=dev.cchqphysics.compat
