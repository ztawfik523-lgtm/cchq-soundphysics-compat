org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# prepareSprCompileJar intentionally resolves the pinned third-party SPR/AT inputs
# at task execution time. The frozen parity workflows already build with the
# configuration cache disabled; keep the reconstruction build reproducible.
org.gradle.configuration-cache=false

# Reconstructed runtime/build baseline
gradle_version=9.2.1
minecraft_version=1.21.1
neo_version=21.1.248
cc_tweaked_version=1.120.2
spr_modrinth_version_id=Dd2tmpsk
cloth_config_curse_file=5729127

# The tested runtime reports HQ's internal mod version as
# 1.1.4-neoforge-1.21.1, but the actual user/runtime JAR filename is the
# Modrinth 1.0.1 NeoForge 1.21.1 file. Pin the immutable Modrinth project and
# version IDs instead of inventing a Maven version from the internal mod version.
hqspeaker_modrinth_project_id=ygA78R8l
hqspeaker_modrinth_version_id=u5PEI5Ax

mod_id=cchq_soundphysics_compat
mod_name=CC:HQ Sound Physics Compat
mod_license=MIT
mod_version=0.1.0
mod_group_id=dev.cchqphysics.compat
